<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alfian.budayaku">

    <queries>

        <!-- Camera -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Budayaku">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".ui.DetailFoodActivity" />
        <activity android:name=".ui.MakananKhasActivity" />
        <activity android:name=".ui.detail.DetailPakaianActivity" />
        <activity android:name=".ui.detail.DetailRumahActivity" />
        <activity android:name=".ui.detail.DetailSenjataActivity" />
        <activity
            android:name=".ui.UploadImageActivity"
            android:theme="@style/Theme.Detail" />
        <activity android:name=".ui.SenjataAdatActivity" />
        <activity android:name=".ui.PakaianAdatActivity" />
        <activity android:name=".ui.RumahAdatActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>